#!/usr/bin/env node

require('./../dist/config')();

/**
 * Start the application
 */
const _ = require('./../dist/app')();
const app = _[0];
const server = _[1];

/**
 * Listen on provided port, on all network interfaces.
 */
server.listen(app.get('port'));
