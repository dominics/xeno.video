var React = require('react');

var ItemList = require('./Channel.jsx');

module.exports = React.createClass({
    render: function () {
        return (
            <ol class="channel-list"></ol>
        )
    }
});