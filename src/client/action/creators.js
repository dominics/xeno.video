import window from './creator/window';
import item from './creator/item';
import channel from './creator/channel';
import setting from './creator/setting';

export default {
  window: window,
  item: item,
  channel: channel,
  setting: setting,
};
